# Environment Configuration
# Copy this file to .env and update with your values

# Application Environment
NODE_ENV=development

# Server Configuration
# API server port (used by pnpm dev and E2E tests)
PORT=4041

# Dev / E2E: override ports if needed (defaults: web 4040, API 4041)
# WEB_PORT=4040
# API_PORT=4041

# Frontend (build-time): API base URL. Default /api (same origin). Set if API is on another domain.
# VITE_API_BASE_URL=/api

# Database Configuration
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://localhost:5432/eutonafila

# Security
# Generate a secure random string (minimum 32 characters)
# In production, use: openssl rand -base64 32
JWT_SECRET=change_me_in_production_use_a_long_random_string_at_least_32_chars

# CORS Configuration
# Allowed origin for CORS requests
CORS_ORIGIN=http://localhost:4040

# Shop Configuration
# Shop identifier/slug
SHOP_SLUG=mineiro

# Storage Configuration (S3-compatible)
# Provider: 's3' (AWS S3), 'r2' (Cloudflare R2), or 'minio' (MinIO)
STORAGE_PROVIDER=s3

# Storage Endpoint
# Required for R2 and MinIO, optional for AWS S3
# Examples:
#   - MinIO: http://localhost:9000
#   - R2: https://<account-id>.r2.cloudflarestorage.com
#   - AWS S3: (leave empty or omit)
STORAGE_ENDPOINT=

# Storage Region
# AWS S3 region (e.g., us-east-1, eu-west-1) or R2 region
STORAGE_REGION=us-east-1

# Storage Bucket Name
# The bucket where ad files will be stored
STORAGE_BUCKET=eutonafila-ads

# Storage Access Credentials
# Access Key ID for your storage provider
STORAGE_ACCESS_KEY_ID=your-access-key-id

# Secret Access Key for your storage provider
STORAGE_SECRET_ACCESS_KEY=your-secret-access-key

# Public Base URL
# Public URL where files can be accessed (CDN or storage public URL)
# Examples:
#   - AWS S3: https://<bucket>.s3.<region>.amazonaws.com
#   - Cloudflare R2: https://<your-domain>.r2.dev or your custom domain
#   - MinIO: http://localhost:9000/<bucket> (if public) or use a reverse proxy
STORAGE_PUBLIC_BASE_URL=https://your-bucket.s3.us-east-1.amazonaws.com
