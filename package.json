{
  "name": "eutonafila",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Queue management system for barbershops",
  "scripts": {
    "dev": "concurrently \"pnpm --filter api dev\" \"pnpm --filter web dev\"",
    "build:shared": "pnpm --filter shared build",
    "build:web": "pnpm --filter web build",
    "build:web:fast": "pnpm --filter web build:fast",
    "generate:avatars": "pnpm --filter api exec tsx apps/api/scripts/generate-barber-avatars.ts",
    "integrate:web": "node scripts/integrate-web.js",
    "build:api": "pnpm --filter api build",
    "build": "pnpm build:shared && pnpm generate:avatars && pnpm build:web && pnpm integrate:web && pnpm build:api",
    "build:fast": "pnpm build:shared && pnpm build:web:fast && pnpm integrate:web && pnpm build:api",
    "start": "pnpm db:migrate && pnpm db:seed && pnpm --filter api start",
    "db:migrate": "pnpm --filter api db:migrate",
    "db:seed": "pnpm --filter api db:seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}

